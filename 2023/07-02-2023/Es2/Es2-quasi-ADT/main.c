#include <stdio.h>
#include <stdlib.h>
#include "tree.h"

int main() {
    // Esempio di utilizzo (creazione manuale possibile perché quasi-ADT)
    /*
    Node n1 = { .value = 5, .num_children = 0, .children = NULL };
    T root = &n1;
    
    int result = f(root);
    printf("Risultato: %d\n", result);
    */
    return 0;
}
